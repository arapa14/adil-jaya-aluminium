# Defaults Storage Structure

Folder `defaults` digunakan untuk menyimpan seluruh asset gambar yang berkaitan dengan default agar mempermudah pengembangan dan menjadi `fallback`.

## Struktur Folder

```plaintext
defaults/